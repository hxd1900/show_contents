# 开发环境会走 Vite 代理，无需下面配置即可请求内网接口。
# 部署到 Netlify/Vercel 等后，需在对应平台「环境变量」里配置，构建时注入：

# 内网 API 根地址（生产环境请求用，同事需连公司内网/ VPN 打开页面）
# 示例：VITE_API_BASE=https://search-aladdin-lamp.hlgdata.com
# VITE_API_BASE=

# 若原版接口不是 /common/template/ids 或 /common/huaban/file/ids，可在原版页 F12 → Network 找返回 JSON 的 XHR，把完整路径填这里：
# 示例：VITE_JSON_API=/api/your/template/batch  或带 {ids}：VITE_JSON_API=/foo/bar/{ids}
# VITE_JSON_API=
