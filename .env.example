# 在浏览器打开原版预览页 → F12 → Network → 筛选 XHR/Fetch → 找到返回 JSON 的请求
# 把路径填在下面（开发环境会自动走 Vite 代理到同域）
# 示例：VITE_JSON_API=/api/your/template/batch
#
# 若接口路径里 ID 在路径中，可用 {ids}：VITE_JSON_API=/foo/bar/{ids}

# VITE_JSON_API=
